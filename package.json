{
  "name": "yt_transcript_fn",
  "version": "1.0.0",
  "description": "Serverless function to transcribe YouTube videos (API wrapper)",
  "main": "functions/hello.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "mkdir -p build/functions && cp -r functions/* build/functions/"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "node-fetch": "^3.3.2",
    "youtube-transcript": "^1.2.1",
    "youtube-transcript-api": "^1.1.2"
  }
}
